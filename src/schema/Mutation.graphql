# import Usuario from 'Usuario.graphql'

type Mutation{
    createUser(nome: String!, email: String!, perfil_id: Int!): Usuario
    deleteUser(id: Int): Usuario
    alterUser(id: Int, nome: String, email: String, perfil_id: Int): Usuario
}